package com.sitepark.ies.aggregator.value.media;

/**
 * Colors extracted from an image. The vibrant color is the one a presentation uses as the image's
 * placeholder or accent color, written as a CSS hex value ({@code #rrggbb}).
 */
public record ColorSwatch(String vibrantColor) {}
