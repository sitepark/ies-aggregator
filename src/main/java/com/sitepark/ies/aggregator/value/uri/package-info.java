/**
 * URI values.
 *
 * <p>The sealed {@link com.sitepark.ies.aggregator.value.uri.Uri Uri} interface spans a value-equal
 * {@link com.sitepark.ies.aggregator.value.uri.PlainUri PlainUri} (rendered verbatim) and {@link
 * com.sitepark.ies.aggregator.value.uri.TranslatableUri TranslatableUri}, whose path segments are
 * individually translatable via the {@code text} package's translation subsystem.
 */
@NullMarked
package com.sitepark.ies.aggregator.value.uri;

import org.jspecify.annotations.NullMarked;
